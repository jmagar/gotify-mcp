{
  "mcpServers": {
    "gotify-mcp": {
      "type": "http",
      "url": "${user_config.gotify_mcp_url}",
      "headers": {
        "Authorization": "Bearer ${user_config.gotify_mcp_token}"
      }
    }
  }
}
